MacOS
-------

# Install Ghidra on MacOS, M2, assumes using homebrew
xcode-select --install
brew install --cask temurin #gets recommended while installing ghidra cask
brew install --cask ghidra
brew install gradle
brew info --cask ghidra #verify


Xcode
-------
Fix Previews behavior and delete cached data to save disk space using the command below in Terminal
xcrun simctl --set previews delete all
